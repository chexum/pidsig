
CMAKE_MINIMUM_REQUIRED(VERSION 2.4)

PROJECT(pidsig C)

SET(PIDSIG_VERSION_MAJOR 0)
SET(PIDSIG_VERSION_MINOR 8)
SET(PIDSIG_VERSION "${PIDSIG_VERSION_MAJOR}.${PIDSIG_VERSION_MINOR}")

ADD_EXECUTABLE(pidsig
  pidsig.c
)

INSTALL(PROGRAMS
  pidsig
  DESTINATION bin)
